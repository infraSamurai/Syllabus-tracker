import { Router } from 'express';

const router = Router();

// Placeholder routes - to be implemented
// router.get('/subjects', protect, getTeacherSubjects);
// router.get('/materials', protect, getTeachingMaterials);
// router.post('/materials', protect, uploadTeachingMaterial);

export default router;